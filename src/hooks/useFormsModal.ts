import {h,ref} from 'vue'
import FormModal from '../components/Modal/forms/index.vue'
interface FormsModalProps{
    title:string
}
const useFormModal = (options={} as FormsModalProps)=>{
    return h('div','1232131312')
}

export default useFormModal
